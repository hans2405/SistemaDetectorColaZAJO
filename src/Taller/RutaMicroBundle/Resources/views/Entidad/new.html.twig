{% extends '::layout.html.twig' %}
{% block title %}Registrar Entidad{% endblock %}
{% block section %}

<div class="row">
    <div class="span3"></div>
    <div class="span4">

        <form action="{{ path('entidad_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
                <p>
                    <button class="btn btn-primary" type="submit"> Registrar </button>
                </p>
            </form>

            <ul class="record_actions">
                <li>
                    <a href="{{ path('entidad') }}">
                        Volver a la Lista de Entidades
                    </a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
