{% extends '::layout.html.twig' %}
{% block title %}Calles{% endblock %}
{% block section%}
    <h2>{{ block('title') }}</h2>

    <table id="example" cellpadding="0" cellspacing="0" border="0" class="display">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Distancia</th>
                <th>Accion</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('calle_show', { 'id': entity.id }) }}">{{ entity.nombre }}</a></td>
                <td>{{ entity.distancia }}</td>
                <td>
               
                        <a href="{{ path('calle_show', { 'id': entity.id }) }}">ver</a>
                   ,
                        <a href="{{ path('calle_edit', { 'id': entity.id }) }}">editar</a>
                  
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

       
            <a class="btn btn-primary" href="{{ path('calle_new') }}">
                Nueva calle
            </a>
     
    {% endblock %}
